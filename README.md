# StrangeAi Studio


This is an AI prompt playground UI built with Vite + React.
It runs independently without Base44 or any third‑party backend by default.

## Running the app

```bash
npm install
npm run dev
```

## Building the app

```bash
npm run build
```

For more information, open an issue or check the docs in `docs/`.